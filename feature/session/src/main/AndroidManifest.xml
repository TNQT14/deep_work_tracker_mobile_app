<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <!-- Keeps a running focus session alive with an ongoing timer notification.
             specialUse subtype "focus_session_timer" (Android 14+ FGS compliance). -->
        <service
            android:name="com.deepworktracker.session.service.FocusSessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="focus_session_timer" />
        </service>

    </application>

</manifest>
